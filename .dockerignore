# 가상환경 / 캐시 — 이미지 안에서 uv sync 로 새로 만든다
.venv/
**/__pycache__/
*.pyc
*.pyo

# 로컬 업로드물 / DB — 볼륨으로 관리
media/
*.sqlite3

# VCS / 에디터 / 문서
.git/
.gitignore
.github/
.vscode/
.idea/
.DS_Store
docs/

# 비밀값은 이미지에 굽지 않는다 (compose environment/env_file 로 주입)
.env

# 테스트 산출물
.pytest_cache/
htmlcov/
.coverage
